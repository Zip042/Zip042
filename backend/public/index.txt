ZIP 042 backend (API only)

  GET /health          헬스체크
  GET /v1/meta         기능 가용성 · 데모 여부
  GET /v1/docs         API 문서

이 프로젝트는 정적 페이지가 없습니다. Vercel 이 api/index.ts 를 직접 컴파일하므로
vercel.json 의 buildCommand 를 비워 둡니다 — 비우지 않으면 npm run build 를 돌린 뒤
public/ 을 찾다가 실패합니다. 이 파일은 그 outputDirectory 를 만족시키기 위한 것입니다.
