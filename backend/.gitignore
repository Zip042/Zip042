# 의존성 · 빌드 산출물
node_modules/
dist/

# ---------------------------------------------------------------------------
# 비밀 값 — 절대 커밋하지 않는다
#
# ⚠️ 저장소에 키를 한 번 커밋하면 히스토리에서 지우기 어렵고, 공개 저장소라면
#    즉시 유출된 것으로 봐야 한다(자동 스캐너가 몇 분 안에 긁어간다).
#    새 키 파일을 만들 때는 반드시 아래 패턴에 걸리는 이름을 쓸 것.
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
.key
*.key
*.pem
secrets/
credentials*.json
service-account*.json

# 도구
.vercel
supabase/.temp/
*.log

# OS
.DS_Store
Thumbs.db
