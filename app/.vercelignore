node_modules
dist
.vercel

# .env.production 은 **일부러 제외하지 않는다**.
# 안에 든 값은 배포된 백엔드의 공개 주소뿐이고(비밀값 아님), Vite 는 이 값을
# 빌드 시점에 번들로 굽는다. .gitignore 만 있으면 Vercel 이 이 파일을 올리지 않아
# VITE_API_BASE_URL 이 빈 채로 빌드되고, 화면이 자기 자신에게 /v1 을 요청하게 된다.
#
# 실제 비밀값(키·토큰)은 절대 여기에 두지 말고 Vercel 프로젝트 환경변수로 넣을 것.
